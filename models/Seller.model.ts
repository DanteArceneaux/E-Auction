//firstName, lastName, address, city, state, pin, phone, email

export class Seller {
    id: number;
    firstName: string;
    lastName: string;
    address: string;
    city: string;
    state: string;
    pin: number;
    phone: number;
    email: string;
}
